kind: Deployment
apiVersion: apps/v1
metadata:
  name: server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: server
  template:
    metadata:
      labels:
        app: server
    spec:
      containers:
        - name: server
          image: 'nikolabod/server'
          resources: 
            requests: 
              memory: "64Mi" 
              cpu: "250m" 
            limits: 
              memory: "128Mi" 
              cpu: "250m"
          ports:
            - containerPort: 4000